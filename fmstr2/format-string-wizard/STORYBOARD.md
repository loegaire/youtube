---
mode: autonomous
message: "Untrusted data becomes dangerous when it is promoted into printf's format recipe."
audience: beginner CTF and pwn learners
duration: 705
music: none
captions: skipped (the visual language carries code labels; owner-voice narration is primary)
---

## Video direction

One continuous leftward camera current joins terminal worlds. The focal mechanical
carrier is the orange parser eye: it scans source code, transforms into a format
token, travels along the argument shelf, then turns into the red write arrow at
`%n`. Scenes remain live through staged code typing, shelf movement, counter fills,
byte flips, and gate mechanics; no idle decorative motion is used.

## Scene map

| Time | Scene | World and visual climax |
|---|---|---|
| 0-5 | `printf` wakes | hello-world terminal; output floats out and energizes the logo |
| 5-15 | universal print | language tiles orbit away, leaving C `printf` as the target |
| 15-17 | man-page flash | `printf(const char *format, ...)` tears free into a machine diagram |
| 17-35 | interpreter | scanner consumes `%d %x %p %s %c %% %08x %20c %n`; `%n` diverts to memory |
| 35-52 | first argument | safe data versus unsafe recipe split-screen |
| 52-80 | target source | `buf` enters first argument; `sus` and win gate become the challenge map |
| 80-110 | endian joke | integer bytes flip into memory order, revealing `sus!` and `flag` |
| 110-130 | normal run | `hello` returns unchanged `sus`; checklist begins |
| 130-160 | probes | `%p.%p.%p` confirms interpretation and leaks shelf-shaped values |
| 160-190 | ghost args | recipe consumes arguments never intentionally passed |
| 190-235 | marker offset | `ABCDEFGH.%14$p` decodes from hex to the first controlled shelf block |
| 235-270 | locate sus | `.data` places `sus` at `0x404060`; `sus+2` targets upper bytes |
| 270-315 | `%hn` | printed-character meter becomes a two-byte write primitive |
| 315-340 | no billion writes | the one-shot counter fails; two rising halfword counts win |
| 340-410 | payload builder | four gears assemble recipe, alignment, and packed address blocks |
| 410-465 | final run | compressed output ribbon drives both writes and opens the branch |
| 465-505 | recap | five mechanical cards connect into one causal pipeline |
| 505-545 | safe patch | `printf("%s", buf)` demotes scary input back into green data |
| 545-575 | read vs write | `%p/%x`, `%s`, and `%n/%hn/%hhn` sort into observe, follow, write |
| 575-610 | count misconception | width specifier pumps the counter; `%hn` copies it |
| 610-640 | positional slots | direct slot 19/20 selection replaces a sequential walk |
| 640-665 | scanf constraint | `%s` stops at whitespace; width formatting creates counter fuel |
| 665-705 | moral | safe and unsafe paths compress into the final teaching sentence |

## Seam ledger

All ordinary seams use a leftward carrier at matched velocity. Chapter changes use the
parser eye, a byte row, or the red write arrow entering 55% through its left-to-right
path. The only reserved Z push is the camera entering `formatted` at 14s and the
payload builder at 340s; both continue forward into the next beat.
